{
    "name": "@db-course/backend",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "private": true,
    "scripts": {
        "dev": "set -a && . ./dev.env && concurrently \"nodemon\" \"nodemon -x tsoa spec-and-routes -c tsoa-config.json\";",
        "lint": "eslint src --ext .ts",
        "lint:fix": "eslint --fix './{src,mysql-migration}/**/*.{js,ts}' && prettier --write './{src,mysql-migration}/**/*.{js,ts}'",
        "codegen": "npm-run-all --parallel codegen:*",
        "codegen:tsoa": "tsoa spec-and-routes -c tsoa-config.json",
        "reset-db:dev": "RDB_ENCRYPTION_KEY=meaningless mysql --host localhost --protocol tcp -u root -pdevpassword -P13306 -e 'DROP DATABASE IF EXISTS dev; CREATE DATABASE IF NOT EXISTS dev; SHOW DATABASES;' && npm run migration:run",
        "docker:dev": "docker-compose up -d",
        "typeorm:dev": "set -a && . ./dev.env && node ./../node_modules/typeorm/cli.js",
        "pretypeorm:dev": "npm run build:compile",
        "migration:generate": "npm run typeorm:dev -- migration:generate",
        "migration:run": "npm run typeorm:dev -- migration:run",
        "migration:show": "npm run typeorm:dev -- migration:show",
        "migration:revert": "npm run typeorm:dev -- migration:revert",
        "precommit": "lint-staged --no-stash",
        "build": "npm run build:compile; npm run build:bundle",
        "build:compile": "tsc --build",
        "build:bundle": "esbuild dist/src/index.js --bundle --outfile=build/index.js --platform=node",
        "start": "npx node build/index.js tsoa routes -c tsoa-config.json"
    },
    "repository": {
        "type": "git",
        "url": ""
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "@types/cors": "^2.8.10",
        "@types/uuid": "^8.3.4",
        "cors": "^2.8.5",
        "esbuild": "^0.12.15",
        "express": "^4.17.1",
        "fabric-ca-client": "^2.2.4",
        "fabric-network": "^2.2.4",
        "fabric-shim": "^2.2.3",
        "inversify": "^6.0.1",
        "inversify-binding-decorators": "^4.0.0",
        "mysql2": "^2.3.0",
        "swagger-ui-express": "^4.1.6",
        "tsoa": "^3.14.1",
        "typeorm": "^0.2.40",
        "typeorm-encrypted": "^0.6.0",
        "typeorm-transactional-cls-hooked": "^0.1.21",
        "typescript-json-schema": "^0.53.1",
        "uuid": "^8.3.2",
        "winston": "^3.3.3"
    },
    "devDependencies": {
        "@types/express": "^4.17.12",
        "@types/jsonwebtoken": "^8.5.5",
        "@types/jwk-to-pem": "^2.0.1",
        "@types/swagger-ui-express": "^4.1.2",
        "concurrently": "^6.2.0",
        "install": "^0.13.0",
        "nodemon": "^2.0.7",
        "ts-jest": "^27.1.4",
        "ts-node": "^10.0.0",
        "tsconfig-paths": "^3.12.0",
        "typescript": "^4.3.2"
    },
    "lint-staged": {
        "**/*.ts": [
            "npm run lint:fix"
        ]
    },
    "jest": {
        "moduleFileExtensions": [
            "ts",
            "js"
        ],
        "transform": {
            "^.+\\.(ts|js)$": [
                "esbuild-jest",
                {
                    "sourcemap": true
                }
            ]
        },
        "globals": {
            "ts-jest": {
                "tsconfig": "tsconfig.json"
            }
        }
    },
    "babel": {
        "presets": [
            "@babel/preset-typescript"
        ]
    }
}
